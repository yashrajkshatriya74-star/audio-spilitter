name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev build-essential ccache

      - name: Install buildozer + cython
        run: |
          pip install --upgrade pip
          pip install buildozer cython==0.29.36

      - name: Build APK with Buildozer (this takes 20-40 minutes)
        run: |
          yes | buildozer android debug
        env:
          # Non-interactive Android SDK license acceptance
          ANDROID_SDK_LICENSE_ACCEPTED: "yes"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AudioSplitterPro-Android-APK
          path: bin/*.apk
